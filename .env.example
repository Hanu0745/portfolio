# Optional. Overrides the production URL (https://hanu.kaziva.in) used for canonical
# links, Open Graph URLs, sitemap.xml and robots.txt, e.g. for a preview deployment.
# NEXT_PUBLIC_SITE_URL=https://preview.example.com
