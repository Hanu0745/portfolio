# Public URL of the deployed site (no trailing slash).
# Used for canonical links, Open Graph URLs, sitemap.xml and robots.txt.
# On Vercel this can be left unset: the production URL is picked up automatically.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
